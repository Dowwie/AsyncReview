# Anthropic API Key (required when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenRouter API Key (required when LLM_PROVIDER=openrouter)
# OPENROUTER_API_KEY=your-openrouter-api-key

# GitHub Token (required for higher rate limits)
GITHUB_TOKEN=your-github-token

# LLM Provider: "anthropic" (default) or "openrouter"
# LLM_PROVIDER=anthropic

# LLM Models (LiteLLM format) — override provider presets
# Anthropic defaults: anthropic/claude-sonnet-4-6 / anthropic/claude-haiku-4-5
# OpenRouter defaults: openrouter/moonshotai/kimi-k2.5 for both
# MAIN_MODEL=anthropic/claude-sonnet-4-6
# SUB_MODEL=anthropic/claude-haiku-4-5

# RLM Configuration
MAX_ITERATIONS=20
MAX_LLM_CALLS=25

# Repo Snapshot Constraints
MAX_FILE_BYTES=200000
MAX_TOTAL_BYTES=5000000

# Optional: Include/Exclude patterns (comma-separated)
# INCLUDE_GLOBS=src/**,lib/**
# EXCLUDE_GLOBS=*.test.ts,*.spec.ts

# Cache directory
CR_CACHE_DIR=~/.cr

# API Server (for Part 2)
API_HOST=127.0.0.1
API_PORT=8000

